https://github.com/zack047/Python--Drowsy-Driver-Detection-and-alert-GUI

py -3.8 -m venv .venv


.venv\Scripts\activate

python -m pip install --upgrade pip

pip install -r requirements.txt

For NVIDIA GPU support on native Windows:
1. Install TensorFlow 2.10.1 from requirements.txt.
2. Install CUDA Toolkit 11.2.
3. Install cuDNN 8.1.
4. Make sure the CUDA and cuDNN bin folders are on PATH.
5. Reopen PowerShell and verify `nvidia-smi` works.

If GPU is still skipped, use the CPU path or run inside WSL2.

streamlit run "Eye_patch_extractor_&_GUI.py"
